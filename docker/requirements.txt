# Python deps for docker/prepare_submission.py.
# Version ranges chosen for broad installability; the exact versions we
# validated the champion 0.00465 submission with are pinned in the technical
# report (Section: Reproducibility) and in models/README.md.

torch>=2.4,<3
timm>=1.0,<1.1
opencv-python-headless>=4.9,<5
albumentations>=1.4,<3
pandas>=2.0,<3
numpy>=1.26,<3
PyYAML>=6.0,<7
tqdm>=4.65
Pillow>=10,<13
# src/dataset.py imports sklearn at module level (fold-splitting helpers,
# unused at inference time but still imported when the module loads).
scikit-learn>=1.3,<2

# Transitive pin: albumentations>=2.0 -> albucore -> stringzilla. Versions
# after 4.6.1 have shipped without a prebuilt manylinux wheel for some
# platforms, forcing a from-source build that needs a C++ toolchain we
# otherwise avoid in the slim runtime image. Pin to the last known-good
# wheel release; the Dockerfile also installs build-essential/cmake as a
# fallback in case this pin ever needs to be relaxed.
stringzilla<=4.6.1
